"""M2.2-M2.5. Triton kernels.

Triton is Linux + NVIDIA only -- these run on the rented box, not the M1.
Every kernel here must be (a) verified numerically against the PyTorch version
and (b) benchmarked against it. A kernel that is fast and wrong is worthless.
"""
